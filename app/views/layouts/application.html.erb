<!DOCTYPE html>
<html>
<head>
  <title>ForgotNotIv</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
<div id="wrapper">
	<div id="logo-area">
		<h1>Forgot Not</h1>
		<br />
		<div id="user-nav">
		  <% if user_signed_in? %>
		    Signed in as <%= current_user.username %>. Not you?
		    <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
		  <% else %>
		    <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %> as a User.
		  <% end %>
		</div>
		<br />
		<div id="keyholder-nav">
		  <% if keyholder_signed_in? %>
		    Signed in as <%= current_keyholder.username %>. Not you?
		    <%= link_to "Sign out", destroy_keyholder_session_path, :method => :delete %>
		  <% else %>
		    <%= link_to "Sign up", new_keyholder_registration_path %> or <%= link_to "sign in", new_keyholder_session_path %> as a Keyholder.
		  <% end %>
		</div>
		<br />
		<div id="guest-nav">
		  <% if guest_signed_in? %>
		    Signed in as <%= current_guest.username %>. Not you?
		    <%= link_to "Sign out", destroy_guest_session_path, :method => :delete %>
		  <% else %>
		    <%= link_to "Sign up", new_guest_registration_path %> or <%= link_to "sign in", new_guest_session_path %> as a Guest.
		  <% end %>
		</div>
	</div>
		<div id="main-menu">
			<ul>
				<li><%= link_to "Home", root_url %></li>
				<li><a href="#">About</a></li>
				<li><a href="#">Examples</a></li>
				<li><a href="#">Forum</a></li>
				<li><a href="#">Help</a></li>
			</ul>
		</div>
		<% if user_signed_in? %>
		<div id="user-menu">
			<ul>
				<li class="btn btn-main"><br /><%= link_to 'My Account', :controller => "/users", :id => current_user.id, :action => "myaccount" %></li>
				<li class="btn btn-main"><br /><%= link_to 'Funeral', 
			      	current_user.funeral.present? ? 
			        funeral_path(current_user.funeral) : 
			        new_funeral_path %></li>
			    <li class="btn btn-main"><br /><%= link_to 'Timeline', 
			      	current_user.timeline.present? ? 
			        timeline_path(current_user.timeline) : 
			        new_timeline_path %></li>
			</ul>
		</div>
		<% end %>
		<div id="wrapper-content">
		
		<p class="notice"><%= notice %></p>

		<%= yield %>
	</div>
</div>

</body>
</html>
